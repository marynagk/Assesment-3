<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :slack_name %>
    <%= form.text_field :slack_name %>
  </div>

  <div class="field">
    <%= form.label :slack_id %>
    <%= form.text_field :slack_id %>
  </div>

  <div class="field">
    <%= form.label :small_avatar %>
    <%= form.text_area :small_avatar %>
  </div>

  <div class="field">
    <%= form.label :big_avatar %>
    <%= form.text_area :big_avatar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
